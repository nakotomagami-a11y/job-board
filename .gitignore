# ───── Dependencies ─────
node_modules/
.pnp
.pnp.js

# ───── Next.js build output ─────
.next/
out/
build/

# ───── Personal user data ─────
# All files inside data/user/ contain personal profile, CV, and job data.
# Copy data/user.example/ → data/user/ to get started.
data/user/

# ───── Auto-apply system (phase 1 + 1.5) ─────
# These are redundant given data/user/ above, but listed explicitly for clarity.
data/user/apply-queue.json
data/user/apply-pack.json
data/user/apply-batch.json
data/user/answer-bank.json
data/user/browser-profile/
data/user/apply-screenshots/

# ───── User-specific docs ─────
# These contain personal search history, company wishlists, and CV data.
# Keep them local only.
docs/CV_REFERENCE.md
docs/SEARCH_LOG.md
docs/SOURCES.md
docs/SEARCH_STRATEGY.md

# ───── Environment / secrets ─────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# ───── Logs ─────
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ───── Editor ─────
.vscode/
.idea/
*.swp
*.swo
*.suo

# ───── OS ─────
.DS_Store
Thumbs.db
desktop.ini

# ───── Claude Code workspace settings ─────
# Project subagents (.claude/agents/) ARE checked in so everyone using the
# repo gets the Haiku scraper. Local user settings (per-session permissions,
# cache, history) stay out.
.claude/settings.json
.claude/settings.local.json
.claude/cache/
.claude/history/

# ───── TypeScript ─────
tsconfig.tsbuildinfo
next-env.d.ts
